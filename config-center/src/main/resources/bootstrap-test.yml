spring:
  application:
    name: config-center
  cloud:
    config:
      server:
        git:
          uri: https://gitlab.com/zgw1469039806/config-center
          clone-on-start: true
          search-paths: test
          #username: ***
          #password: ***
server:
  port: 9999